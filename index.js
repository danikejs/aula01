//let metas = [" abdalla","alo"]
//console.log(metas[1]+','+metas[0])

//arrays, objetos
let meta = {
    value: 'ler um livro todo mes',
    //boolean
    checked:false,
    log: (info) => {
        console.log(info)
    }
}

meta.value = "ir para academia "
meta.log(meta.value)

//function    // arrow  function
//const criarMeta = () => {}

//function criarMeta() {}